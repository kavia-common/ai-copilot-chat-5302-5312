# PUBLIC_INTERFACE
# Example environment for the AI Copilot Frontend.
# Copy this file to ".env" and adjust values as needed.

# Base URL of the backend API. If omitted, the app will default to http://localhost:3001.
# During development, ensure your backend runs on port 3001.
REACT_APP_API_BASE_URL=http://localhost:3001
